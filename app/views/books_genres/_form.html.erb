<%= form_with(model: books_genre) do |form| %>
  <% if books_genre.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(books_genre.errors.count, "error") %> prohibited this books_genre from being saved:</h2>

      <ul>
        <% books_genre.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :genres_id, style: "display: block" %>
    <%= form.text_field :genres_id %>
  </div>

  <div>
    <%= form.label :books_id, style: "display: block" %>
    <%= form.text_field :books_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
